# Demo Configuration for Mock Datadog Server
# Add these to your .env file

# For demo with mock server, you can use placeholder values
# The mock server accepts any API key (or no key) for demo purposes
DD_API_KEY=demo_api_key_12345
DD_APP_KEY=demo_app_key_67890

# Point to the mock server
DD_MOCK_SERVER=http://localhost:8080

# Use "production" mode (not "demo") to route to mock server
AGENT_ENV=production

# Keep your other settings
DD_SITE=datadoghq.com
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0

